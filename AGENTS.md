# AGENTS.md — CareFlow 개발 협업 규칙 (Codex/AI 필독)

## 0. 시작 전 반드시
이 저장소에서 작업하기 전 `docs/SPEC.md`를 읽는다. 모든 구현은 SPEC + 〈사회복귀지표 설계안〉을 따른다.

## 1. 절대 경계 (위반 시 작업 거부)
- 비의료기기. **진단/중증도 판정/예후 예측/치료·재활 처방 코드·카피 금지.**
- 사용자 대상 문구는 단정·감정규정 금지, 질문형, "고려해보세요/함께 볼까요". 상관을 인과로 표현 금지.
- 위험 신호 → 의료진·외부자원 연계 분기 포함.

## 2. 스택
- 웹: Next.js(App Router, TS) + Tailwind, 배포 Vercel. 모바일: Expo RN(careflow-app, 동일 Supabase 공유).
- 백엔드: Supabase(Postgres+Auth+RLS). 스키마: `supabase/schema.sql`.
- 비밀키는 .env(커밋 금지). RLS로 본인 행만 접근.

## 3. 작업 방식
- 작은 단위(한 화면/한 API/한 테이블)로 PR. PR 설명에 SPEC 어느 항목인지 명시.
- 지표는 개인 7일 기준선 대비 band(low/normal/high)로만. 임상 컷오프 하드코딩 금지.
- 검증: 타입체크/빌드 통과 + 사용자 카피 경계 셀프체크.

## 4. 역할
- 사람(정유진): 제품·임상·카피 최종 검수, 머지.
- Claude: 스펙·설계·데이터/지표 로직·리뷰·복잡 작업.
- Codex: 반복 구현(CRUD·폼·뷰·테스트)을 SPEC 기준으로.
